@using HotelProject.WebUI.Models.SearchRoom
@model List<ResultRoomListExampleData>

@{
    Layout = null;

  
}

<!DOCTYPE html>
<html lang="en">

@await Component.InvokeAsync("_HeadPartial")

<body>
    <div class="bg-white p-1">

        @await Component.InvokeAsync("_SpinnerPartial")

        @await Component.InvokeAsync("_NavbarPartial")
        @await Component.InvokeAsync("_RoomCoverPartial")

        <!-- Booking End -->
        <!-- Room Start -->
        <div class="container-xxl py-5">
            <div class="container">
                <div class="text-center wow fadeInUp" data-wow-delay="0.1s">
                    <h6 class="section-title text-center text-primary text-uppercase">Otellerimiz</h6>
                    <h1 class="mb-5">Uygun Oteli Anında <span class="text-primary text-uppercase">Bulun</span></h1>
                </div>
                <div class="row g-4">
                    @foreach(var item in Model)
                    {
                      @*   var label = @item.accessibilityLabel;

                        var yatakSayisi = "";

                        if (label.Contains("bed"))
                        {
                            var index = label.IndexOf("bed");
                            var start = label.LastIndexOf(" ", index - 2); // sayının başladığı yer
                            yatakSayisi = label.Substring(start, index - start).Trim(); // örneğin: "1"
                        } *@

                        <div class="col-lg-4 col-md-6 wow fadeInUp" data-wow-delay="0.1s">
                            <div class="room-item shadow rounded overflow-hidden">
                                <div class="position-relative">
                                    <img class="img-fluid" src="@item.property.photoUrls[0]" alt="">
@*                                     <small class="position-absolute start-0 top-100 translate-middle-y bg-primary text-white rounded py-1 px-3 ms-4">$ @item.property.priceBreakdown.grossPrice.value /Night</small>
 *@                                </div>
                                <div class="p-4 mt-2">
                                 @*    <div class="d-flex justify-content-between mb-3">
                                        <h5 class="mb-0">@item.property.name</h5>
                                        <div class="ps-2">
                                            <small class="fa fa-star text-primary"></small>
                                            <small class="fa fa-star text-primary"></small>
                                            <small class="fa fa-star text-primary"></small>
                                            <small class="fa fa-star text-primary"></small>
                                            <small class="fa fa-star text-primary"></small>
                                        </div>
                                    </div>
                                    <div class="d-flex mb-3">
                                        <small class="border-end me-3 pe-3"><i class="fa fa-bed text-primary me-2"></i>@yatakSayisi Bed</small>
                                        <small class="border-end me-3 pe-3"><i class="fa fa-bath text-primary me-2"></i>2 Bath</small>
                                        <small><i class="fa fa-wifi text-primary me-2"></i>Wifi</small>
                                    </div> *@
                                    <div class="card shadow-sm p-3 mb-4">
                                        <h5 class="card-title text-primary">@item.property.name</h5>
                                        <div class="card-text">
                                            @foreach (var line in item.accessibilityLabel.Split('\n'))
                                            {
                                                if (!string.IsNullOrWhiteSpace(line))
                                                {
                                                    <p class="mb-1 text-secondary">• @line.Trim()</p>
                                                }
                                            }
                                        </div>
                                        <br />
                                        <a class="btn btn-sm btn-primary rounded py-2 px-4" asp-controller="Room" asp-action="RoomList" >Detayına Git</a>

                                    </div>

                                   
                                </div>
                            </div>
                        </div>

                    }
                 
                </div>
            </div>
        </div>
        <!-- Room End -->
        @await Html.PartialAsync("/Views/Default/_SubscribePartial.cshtml")

        @await Component.InvokeAsync("_FooterPartial")

        <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>
    </div>
    @await Component.InvokeAsync("_ScriptsPartial")
</body>
</html>